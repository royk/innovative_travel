{{#hotel-auto-complete options=hotels selectedValue=model.name placeHolderText="Start typing to search..." noMesssagePlaceHolderText="No hotels found" as |result|}}
  {{entry-icon type=result.subtype}}
  <div>
    {{result.name}}
  </div>
{{/hotel-auto-complete}}
{{outlet}}
